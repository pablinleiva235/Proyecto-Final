# `pyqt_gui.py`

Aca va el codigo de la interfaz grafica diseñada en PyQt5Designer, que genera un archivo .ui que se debe pasar a .py con el siguiente comando:

```bash
pyuic5 -x pyqt_gui.ui -o pyqt_gui.py
```

Al hacer modificaciones debe pegarse todo el codigo nuevo en pyqt_gui.py